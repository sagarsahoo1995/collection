1. Access Modifies:

  i) private: Access only within the class[methods, variable].
  
  ii) protected : declarations are visible within the package or all subclasses
  
  iii) default : declarations are visible only within the package 
  
  iv) public : declarations are visible everywhere